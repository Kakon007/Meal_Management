import 'package:flutter/material.dart';

class SignInWithGoole extends StatelessWidget {
  const SignInWithGoole({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}